<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jahanveenarang.smyle">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".Facebook" />
        <activity android:name=".DashBoard" />
        <activity
            android:name=".AboutActivity"
            android:parentActivityName=".DashBoard">

        </activity>
        <activity android:name=".Contributions" />
        <activity android:name=".Supporters" />
        <activity android:name=".JoinUS" />
        <activity android:name=".GiveEducation100children">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>